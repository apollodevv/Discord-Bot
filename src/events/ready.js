module.exports = async (client) => {
    console.log(`Logged as: ${client.user.username}`);

    /**
     * Interactions: discord.js v13
     * 
     * const data = {
     *      name: 'stats',
     *      description: 'Get information about the bot',
     * };
     * 
     * const command = await client.application?.commands.create(data);
     */
};